CREATE TABLE usuarios( id INT UNSIGNED PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100) NOT NULL, email VARCHAR(50) NOT NULL, username VARCHAR(50) NOT NULL, password VARCHAR(150) NOT NULL, genero VARCHAR(50) NOT NULL, age INT NOT NULL, ext_img VARCHAR(4) NOT NULL);
